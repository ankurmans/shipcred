// Force dynamic rendering — this page requires Supabase client at runtime
export const dynamic = 'force-dynamic';

export default function OnboardingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
